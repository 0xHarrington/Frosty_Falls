class Block {
  float cx, cy;
  // light
  // collision
  // motion
  // path(?)
  // velocity / speed
  
  
  Block(float x, float y) {
    cx = x;
    cy = y;
  }
  
  void updateVelovity() {}
  void updateLocation(float dt) {}
  void show() {}
}
